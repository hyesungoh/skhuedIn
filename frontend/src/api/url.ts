const baseUrl: string = "http://localhost:8080";

export const GET_BLOGS: string = `${baseUrl}/api/blogs`;

// Social Login
export const LOGIN_GOOGLE: string = `${baseUrl}/auth/google/callback`;
export const LOGIN_KAKAO: string = `${baseUrl}/auth/kakao/callback`;
